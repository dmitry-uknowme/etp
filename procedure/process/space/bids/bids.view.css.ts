namespace $.$$ {
  const { rem } = $mol_style_unit;

  $mol_style_define($etp_procedure_process_space_bids, {
    Body: {
      padding: $mol_gap.block,
    },

    // flex: {
    //   basis: rem(40),
    // },

    Foot: {
      padding: $mol_gap.block,
    },
  });
}
